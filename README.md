This project implements fundamental graph traversal and shortest path algorithms to find routes between cities.
Each city is represented with geographic coordinates (latitude, longitude), and roads are modeled as edges with distances.
Algorithms Used:
-BFS (Breadth-First Search)
-Dijkstra Algorithm
-A* (A-Star) Algorithm
